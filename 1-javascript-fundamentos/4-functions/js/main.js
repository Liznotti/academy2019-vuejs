/*
  soma
  subtrai
  multiplica
  divide
  full-name
*/
  // ------------ 1 FORMA -----------------
  // function minhaFuncao(name) {
  // return `Olá, ${name}`
  // return 'Olá ' + name

// } ------------ 2 FORMA -----------------

 // const minhaFuncao = function (name) {
 // return `Olá, ${name}'

// }  ------------ 3 FORMA -----------------

  // const minhaFuncao = name => `Olá, ${name}`
  // console.log(minhaFuncao('Breno'))

var valorX = 5, valorY = 10

let soma = valorX + valorY
let subtrai = valorX - valorY
let multiplica = valorX * valorY
let divide = valorX / valorY

function funcaoSoma(soma) {
  return soma
  }

function funcaoSubtrai(subtrai) {
  return subtrai
  }

function funcaoMultiplica(multiplica) {
  return multiplica
  } 

function funcaoDivide(divide) {
  return divide
  } 